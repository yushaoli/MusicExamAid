{
  "name": "musicexamaid",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build -w @musicexamaid/shared && npm run build -w @musicexamaid/api",
    "test": "npm test -w @musicexamaid/shared",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check-shared": "./scripts/check-shared.sh"
  }
}
